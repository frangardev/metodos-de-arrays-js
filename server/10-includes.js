//Includes nos dice si un elemento forma parte del array
//Include no recibe una funcion, recibe un parametro
//Includes tambien funciona para strings 

const pets = ['Cat', 'Dog', 'Bat', 'Rabbit']

const areCat = pets.includes('Cat')

console.log(areCat);